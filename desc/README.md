# Minimal DESC reimplementation (fast)

**Run**

```bash
pip install -r requirements.txt
python3 download_datasets.py
    #You now have pbmc3k.h5ad and blobs.h5ad in data/
python3 train_desc.py --config config.yaml
```
